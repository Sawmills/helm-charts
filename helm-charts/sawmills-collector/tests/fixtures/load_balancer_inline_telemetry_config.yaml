telemetryConfig:
  exporters:
    debug/main_marker: {}
  service:
    pipelines:
      metrics/main_marker:
        receivers: []
        exporters:
          - debug/main_marker

loadBalancer:
  telemetryConfig:
    exporters:
      debug/lb_marker: {}
    service:
      pipelines:
        metrics/lb_marker:
          receivers: []
          exporters:
            - debug/lb_marker
